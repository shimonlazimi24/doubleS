/**
 * Layout primitives — align with `--space-*` in design-tokens.css (8px system).
 */

/** Horizontal gutters for shell / container */
export const containerPaddingX = "px-ds-3 md:px-ds-4";

/** Legacy alias (same as container horizontal padding). */
export const shellPaddingX = containerPaddingX;

/** Hero vertical rhythm inside marketing hero */
export const heroPadding = "pb-ds-10 pt-ds-8 md:pb-ds-12 md:pt-ds-10";

/** Stack gap between major blocks on a page */
export const stackGapSection = "gap-ds-8 md:gap-ds-10";
