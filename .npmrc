# vitest@2 (via @vitest/mocker) מצהיר peer על vite@5 בעוד חלק מהעץ מבקש אחרת —
# קונפליקט peer ידוע שלא משפיע בפועל. legacy-peer-deps מיישר את ההתקנה
# מקומית וב-CI (npm ci קורא את הקובץ הזה) כך שה-lockfile נשאר עקבי.
legacy-peer-deps=true
